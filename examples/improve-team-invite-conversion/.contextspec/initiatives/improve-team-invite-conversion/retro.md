# Retro

The initiative is mid-flight. This file is a placeholder.

When phases complete, a retro should record:

- What shipped (per phase).
- What did not ship and why.
- Final headline metric vs. target.
- Per-phase decision recorded against `plan.md`.
- Proposed updates to `memory/lessons.md`, `memory/experiment-results.md`, `memory/anti-patterns.md`.
- Proposed updates to `domains/onboarding/decisions.md` if any durable decision emerged.
- Role-checklist updates: any common mistake observed should be added to the relevant role's `## Common Mistakes`.

All proposed updates require human confirmation before being written, per spec §11 rule 5.
