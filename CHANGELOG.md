# Changelog

All notable changes to ContextSpec will be documented in this file.

## v0.1.0

- PR #4: ship deterministic context pack compilation for role-scoped task handoffs and reviews.
- PR #7: add `init` and `create initiative` scaffolding for a working `.contextspec/` setup.
- PR #8: generate Claude slash commands and Codex `AGENTS.md` from the registry.
- finish-line: add dogfood coverage, `contextspec validate`, and release hardening for the first npm publish.
